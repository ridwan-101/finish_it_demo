import 'package:flutter/material.dart';

class CompressedResult extends StatefulWidget {
  const CompressedResult({super.key});

  @override
  State<CompressedResult> createState() => _CompressedResultState();
}

class _CompressedResultState extends State<CompressedResult> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(

        
      ),
    );
  }
}
